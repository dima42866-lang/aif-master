--- aif	2020-04-05 14:37:05.587776000 +0300
+++ aif-master	2020-04-06 09:38:33.623408360 +0300
@@ -22,13 +22,13 @@
 # Save retyping
 VERSION="MAXIMALISIMUS Installation Framework 2.6"
 
-_src_fls=$(find "$filesdir/config" -maxdepth 1 -type f -iname "*.sh")
+_src_fls=$(find "/usr/share/aif-master/config" -maxdepth 1 -type f -iname "*.sh")
 wait
 for i in ${_src_fls[*]}; do
     source "$i"
     wait
 done
-_src_fls=$(find "$filesdir/modules" -maxdepth 1 -type f -iname "*.sh")
+_src_fls=$(find "/usr/share/aif-master/modules" -maxdepth 1 -type f -iname "*.sh")
 wait
 for i in ${_src_fls[*]}; do
     source "$i"
